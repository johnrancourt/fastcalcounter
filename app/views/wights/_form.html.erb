<%= form_for(@wight) do |f| %>
  <% if @wight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wight.errors.count, "error") %> prohibited this wight from being saved:</h2>

      <ul>
      <% @wight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :wegiht %><br>
    <%= f.number_field :wegiht %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
